import { history } from '@/utils/history'
import {
  setToken,
  getToken,
  removeToken, isLogin
} from '@/utils/token'
import http from './http'
export { setToken, getToken, removeToken, isLogin }
export { history }
export { http }